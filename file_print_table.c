#include <stdio.h>
int main()
{
    FILE *ptr;
    int num;

    printf("Enter number to print table : ");
    scanf("%d", &num);

    ptr = fopen("table.txt", "w");
    for(int  i=0; i<10;++i)
    {
        fprintf
    }

    return 0;
}